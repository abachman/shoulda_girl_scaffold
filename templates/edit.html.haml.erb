- content_for :title, "Edit <%= class_name %>"
- form_for @<%= singular_name %>, :url => { :action => :update } do |f|
  = error_messages_for :<%= singular_name %>
  = render :partial => 'fields', :locals => {:f => f}
  %p
    = f.submit
    = link_to "Cancel", <%= index_path %>

